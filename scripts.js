document.addEventListener('DOMContentLoaded', () => {
  console.log('DaemonBSD site loaded!');
  const btn = document.querySelector('.download-btn');
  btn.addEventListener('click', () => alert('Downloading DaemonBSD ISO! 🔥'));
});
